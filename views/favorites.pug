extends layout.pug
block content
  h1 Your Favorite Parks!
  ul
    each favorite in favorites
      -let url = `http://localhost:8080/parks/` + favorite.parkId
      a(href=url) #{favorite.name}
      br